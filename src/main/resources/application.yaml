spring:
  application:
    name: Food Delivery
  datasource:
    url: jdbc:mysql://localhost:3306/foodDB?createDatabaseIfNotExist=true
    username: root
    password: admin
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 300 #5m
    refreshTokenExpiration: 604800 #7d